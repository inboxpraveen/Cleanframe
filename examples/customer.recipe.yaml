# CleanFrame recipe — generated by CleanFrame, edited by you, owned by git.
# Docs: https://github.com/inboxpraveen/Cleanframe/wiki  |  format v1
version: 1
source_fingerprint:
  columns: 6
  column_names:
  - Customer Name
  - Signup Date
  - Amount
  - City
  - Email
  - Phone
  dtypes:
    Customer Name: object
    Signup Date: object
    Amount: object
    City: object
    Email: object
    Phone: object
  row_count: 6
  sampled_rows: 6
  hash_sample: 09264e6b98b451bc
columns:
  Customer Name:
    rename_to: customer_name
    ops:
    - collapse_whitespace
  Signup Date:
    rename_to: signup_date
    ops:
    - parse_date:
        formats:
        - '%Y-%m-%d'
        - '%d/%m/%Y'
        - '%d %b %Y'
  Amount:
    rename_to: amount_inr
    ops:
    - parse_number
  City:
    rename_to: city
    ops:
    - strip_whitespace
    - normalize_values:
        MUMBAI: Mumbai
        bengaluru: Bengaluru
  Email:
    rename_to: email
    ops:
    - normalize_email
  Phone:
    rename_to: phone
    ops:
    - normalize_phone
validate:
- column: customer_name
  check: not_null
  on_fail: quarantine
- column: signup_date
  check: not_null
  on_fail: quarantine
- column: amount_inr
  check: not_null
  on_fail: quarantine
- column: amount_inr
  check: '>= 0'
  on_fail: quarantine
- column: city
  check: in
  on_fail: quarantine
  values:
  - Bangalore
  - Mumbai
  - Delhi
- column: email
  check: not_null
  on_fail: quarantine
- column: email
  check: valid_email
  on_fail: quarantine
- column: phone
  check: valid_phone
  on_fail: quarantine
meta:
  created_with: cleanframe 0.1.0
  generated_by: rules
  mode: auto
  notes:
  - '2 low-confidence fix(es) omitted under mode=auto (threshold 0.65): inconsistent_case'
  - '1 issue(s) need review (no automatic fix): invalid_emails'
